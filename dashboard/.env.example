VITE_API_BASE=http://localhost:8080

# Must match ADMIN_API_TOKEN on the backend when that is set, otherwise the admin panel on the
# Gateway page gets 401. This value is baked into the built bundle and is readable by anyone who
# loads the page, so treat it as a speed bump for a public demo, not as a secret.
VITE_ADMIN_TOKEN=
